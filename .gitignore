# ── Editor swap files / backups ──────────────────────────────────────
*.sw?
*.swp
*.swo
*.*~
*~
.netrwhist

# ── Build output ─────────────────────────────────────────────────────
/out
/build
/packpack
dist/appimage/build/

# ── Compiled / generated ─────────────────────────────────────────────
lib/lib
lib/res
blib/
pm_to_blib

# ── Coverage / profiling ─────────────────────────────────────────────
cover_db/
coverage.txt
nytprof/
nytprof.out

# ── Local CPAN install (cpanm --local-lib) ───────────────────────────
local/
perl5/

# ── Debian build artifacts ───────────────────────────────────────────
*.deb
*.rpm
*.AppImage
*.changes
*.buildinfo
*.dsc
*.tar.gz
*.tar.xz
debian/files
debian/tmp/
debian/*.debhelper
debian/*.substvars
debian/debhelper-build-stamp

# ── Claude Code / agent scratch ──────────────────────────────────────
.claude/
tasks/
CLAUDE.md
*.claude

# ── IDE / editors ────────────────────────────────────────────────────
.idea/
.vscode/
*.code-workspace
.kdev4/
*.kdev*
.project
.settings/
.DS_Store
Thumbs.db

# ── OS ───────────────────────────────────────────────────────────────
*.pid
core
core.[0-9]*

# ── Logs ─────────────────────────────────────────────────────────────
*.log
logs/

# ── Temp / scratch ───────────────────────────────────────────────────
*.tmp
*.bak
*.orig
*.rej
.scratch/
TODO.local
NOTES.local

# ── Secrets (defensive — should never be in repo) ────────────────────
*.pem
*.key
*.p12
.env
.env.local
.netrc
